{
  "name": "ai-browser",
  "version": "0.1.0",
  "private": true,
  "description": "Passive web extraction engine for AI-readable JSON and Markdown.",
  "type": "module",
  "scripts": {
    "extract": "tsx src/cli.ts",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.3"
  }
}
